# Css fonts

Font properties:
* `font`: shorthand for:
* `font-family`:
* `font-size`:
* `font-style`:
* `font-variant`:
* `font-weight`:

Text properties:
* `letter-spacing`:
* `line-height`:
* `text-align`:
* `text-decoration`:
* `text-indent`:
* `text-shadow`:
* `text-transform`:
* `white-space`:
* `word-spacing`:

Direction:
* `direction`:
* `unicode-bidi`:
