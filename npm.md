# Npm

```bash
# quick npm init
npm init -y

# install, save dependencies
$ npm i -s mymodule

# global install
$ npm i -g mymodule

# install, save in developer's dependencies
$ npm i --save-dev mymodule
```
